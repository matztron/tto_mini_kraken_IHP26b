# Shared scripts

| File | Role |
|------|------|
| [`kraken_pin_protocol.py`](kraken_pin_protocol.py) | Pin-loader opcodes + `uio`/`ui` packing (SDK **and** cocotb) |

Edit this module when the pin protocol changes — not a copy under `sdk/` or `test/`.
